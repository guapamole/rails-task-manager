<h1><%= @task.title %></h1>
<p><%= @task.details %></p>

<%= link_to 'Edit task', edit_task_path(@task), data: { turbo_method: :get } %>
<br>
<%= link_to "Delete task", destroy_task_path(@task), data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %>
<br>
<%= link_to "Return to the tasks list", tasks_path %>
